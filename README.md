# BuildingInterior

A prototype of the building creation workflow, made to write the spec before we build it in Unity.

- `prototype/index.html`: self-contained three.js prototype (edit mode + play mode). Open it in a browser; it loads three.js from jsDelivr.
- `docs/SPEC.md`: findings, data model, occlusion design, and the Unity implementation plan and open questions.
